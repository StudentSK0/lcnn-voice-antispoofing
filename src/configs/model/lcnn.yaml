_target_: src.model.VoiceAntiSpoofingModel
n_fft: ${audio.n_fft}
win_length: ${audio.win_length}
hop_length: ${audio.hop_length}
n_frames: ${audio.n_frames}
stft_eps: 1e-12
dropout_probability: 0.75
num_classes: 2
